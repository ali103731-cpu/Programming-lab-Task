Input:

# 7. Usernames and passwords - print usernames with passwords longer than 8 characters
users = {
    "ali_khan": "ali123",
    "admin_pk": "Pakistan@2024",
    "guest": "guest",
    "sara_ahmed": "MyPass@word123",
    "test_user": "test99"
}

print("Usernames with passwords longer than 8 characters:")
items = list(users.items())
i = 0
while i < len(items):
    username, password = items[i]
    if len(password) > 8:
        print(username)
    i += 1

Output:


Usernames with passwords longer than 8 characters:
admin_pk
sara_ahmed
